"""Kernel/ORM integration helpers."""

from __future__ import annotations

from pathlib import Path


def validate_schema_snapshots(workspace_root: Path) -> None:
    """Validate ORM alignment between CLI and Kernel layers."""

    raise NotImplementedError("validate_schema_snapshots pending implementation.")
